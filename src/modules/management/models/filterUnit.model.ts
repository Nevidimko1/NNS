export interface IFilterUnitsModel {
    filters: {
        filterById: string;
        filterByCity: string;
        filterByName: string;
        filterBySize: string;
        filterByProducts: string;
    };
}
